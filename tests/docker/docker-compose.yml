## MediaWiki with MariaDB, Access via "http://localhost:8080"

services:
  mediawiki:
    image: mediawiki
    working_dir: /var/www/html
    restart: always
    ports:
      - '8080:80'
    volumes:
      - /var/www/html/images
      - /var/www/html/data
      - ./mediawiki-settings.php:/var/www/html/mediawiki-settings.php
      - ./main.sh:/var/www/html/main.sh
      - ./setup.sh:/var/www/html/setup.sh
